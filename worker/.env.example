# Copy to worker/.env and fill in. That file is gitignored and belongs on the maintainer's machine
# and nowhere else — no CI, no repo secret, no password manager sync into a shared vault.
#
# The Discord webhook is deliberately absent. It lives only in Cloudflare, set by rotate-webhook.sh.

CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
