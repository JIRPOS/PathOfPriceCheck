/build/
/build-*/
/dist/
/out/
.cache/
compile_commands.json
*.user
.DS_Store

# Runtime-generated ImGui layout
imgui.ini

# Fontin is free to use but not to redistribute — fetched, never committed.
assets/fonts/*.ttf
assets/fonts/*.otf
assets/fonts/Fontin-LICENSE.txt

# built website
_site/

# Report relay: deploy credentials are local-only, and so is anything wrangler leaves behind.
worker/.env
worker/.dev.vars
worker/node_modules/
worker/.wrangler/

# Discord report ingestion: local-only bot credential and pre-processed report content.
discord-reports/.env
discord-reports/inbox/
